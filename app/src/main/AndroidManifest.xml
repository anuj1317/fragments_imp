<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fragments_imp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fragments_imp">
        <activity
            android:name=".preference_Activity"
            android:exported="true"
            android:label="@string/title_activity_preference"
            android:theme="@style/Theme.Fragments_imp.NoActionBar" />
        <activity
            android:name=".fragdemo"
            android:exported="true" />
        <activity
            android:name=".Initial_Activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>